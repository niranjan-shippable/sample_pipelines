jobs:
  - name: runSh-Job-N
    type: runSh
    steps:
      - TASK:
        - script: echo KEY=value
        - script: sudo apt-get install tree
        - script: tree -F IN
        - script: sleep 3
        - echo $KEY
    on_success:
      - script: echo "successfully completed"
