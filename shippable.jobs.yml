jobs:
  - name: runSh-Job
    type: runSh
    steps:
#      - IN: nginx-image
      - TASK:
        - script: docker pull shipimg/genexec:v5.2.3
    on_success:
      - script: echo "successfully completed"

  - name: sample_java_runCI
    type: runCI
    steps:
      - OUT: nginx-image

  - name: nginx-manifest
    type: manifest
    steps:
      - IN: nginx-image
