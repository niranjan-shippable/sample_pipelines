resources:
  - name: nginx-image-params
    type: image
    integration: "docker-subint"
    pointer:
      sourceName: "ankul/nginx-X"
    seed:
      versionName: prod.10

  - name: params-params
    type: params
    version:
      params:
        MONGO_API_URL: "localhost:28019"
